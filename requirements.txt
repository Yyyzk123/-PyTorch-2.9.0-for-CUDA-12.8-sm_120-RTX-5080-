# Minimal dependencies for demo_tensor_cuda.py
# Torch is installed via .whl (do NOT include it here)

typing-extensions==4.7.1     # Verified with PyTorch 2.9.0
filelock==3.12.2             # Used in torch.utils.model_zoo
jinja2==3.1.3                # For config or template rendering
fsspec==2023.6.0             # For file system abstraction
sympy==1.12                  # Used by torch.fx symbolic tracing
networkx==3.1                # Required by torch.fx
mpmath==1.3.0                # For sympy symbolic math
